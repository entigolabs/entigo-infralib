apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: test
spec:
  parentRefs:
  - name: test
  hostnames:
  - test.infralib.entigo.io
  rules:
  - backendRefs:
    - name: test
      port: 80
