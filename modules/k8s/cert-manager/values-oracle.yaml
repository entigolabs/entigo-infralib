issuer:
  # modules/k8s/ingress-nginx - the HTTP-01 solver ingress is served by nginx like
  # any other ingress; no listener-port or NodePort special-casing needed (both were
  # OCI Native Ingress Controller quirks).
  ingressClassName: nginx
