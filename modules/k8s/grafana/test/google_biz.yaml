global:
  google:
    hostname: grafana.{{ .toutput.dns.pub_domain }}
    gateway:
      name: "google-gateway-external"
grafana:
  grafana.ini:
    server:
      root_url: https://grafana.{{ .toutput.dns.pub_domain }}
