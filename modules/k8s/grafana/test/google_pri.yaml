global:
  google:
    hostname: "{{ .tsmodule.grafana }}.{{ .toutput.dns.pub_domain }}"
    gateway:
      name: "google-gateway-external"
grafana:
  grafana.ini:
    server:
      root_url: https://{{ .tsmodule.grafana }}.{{ .toutput.dns.pub_domain }}
