{{ if index .Values "grafana" "enabled" }}
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.awsAccount }}:role/{{ .Release.Name }}-sm
  name: {{ .Release.Name }}-sm
{{ end }}
