{{- if .Values.installCompositeResources }}
apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: compositecronjobs.workload.entigo.com
spec:
  compositeTypeRef:
    apiVersion: workload.entigo.com/v1alpha1
    kind: CompositeCronJob
  mode: Pipeline
  pipeline:
    - step: cron-job
      functionRef:
        name: developer-portal-function
      input:
        apiVersion: template.fn.crossplane.io/v1beta1
        kind: Input
{{- end }}
