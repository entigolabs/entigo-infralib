{{- if .Values.installCompositeResources }}
apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: postgresqls.database.entigo.com
spec:
  compositeTypeRef:
    apiVersion: database.entigo.com/v1alpha1
    kind: PostgreSQL
  mode: Pipeline
  pipeline:
    - step: postgresql
      functionRef:
        name: developer-portal-function
{{- end }}