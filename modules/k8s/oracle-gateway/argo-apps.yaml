metadata:
  annotations:
    # After istio-base/istio-istiod (1) and oci-ingress (2, which also ships the Gateway
    # API CRDs - no separate gateway-api-crds module needed here).
    infralib.entigo.io/sync-wave: "3"
spec:
  # See templates/secret.yaml - genSelfSignedCert churns every render, ignored for the
  # same reason oci-native-ingress-controller's webhook cert ignores its own.
  ignoreDifferences:
    - group: ""
      kind: Secret
      jsonPointers:
        - /data
  syncPolicy:
    syncOptions:
    - ServerSideApply=true
    managedNamespaceMetadata:
      labels:
        pod-security.kubernetes.io/enforce: restricted
        pod-security.kubernetes.io/warn: restricted
        tenancy.entigo.com/zone: infralib
