external-secrets:
  env:
  - value: "{{ .toutput.eks.region }}"
    name: "AWS_DEFAULT_REGION"
global:
  createClusterSecretStore: true
  enableWriteForClusterSecretStore: true
  createPasswordGenerator: true
