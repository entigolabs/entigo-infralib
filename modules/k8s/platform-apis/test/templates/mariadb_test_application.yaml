apiVersion: v1
kind: Namespace
metadata:
  name: test-mariadb
  labels:
    tenancy.entigo.com/zone: a
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: baseline
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test-mariadb
  namespace: argocd-biz
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: test-mariadb
  project: a
  source:
    chart: platform-apis-test-mariadb
    repoURL: ghcr.io/entigolabs
    targetRevision: 'v0.2.35'
    helm:
      releaseName: mariadb
      values: |
        targetRevision: 'v0.2.35'
  ignoreDifferences:
    - group: "database.entigo.com"
      kind: MariaDBInstance
      jsonPointers:
        - /spec/deletionProtection
  syncPolicy:
    syncOptions:
      - RespectIgnoreDifferences=true
