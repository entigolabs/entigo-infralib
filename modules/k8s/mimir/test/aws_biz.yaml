mimir-distributed:
  gateway:
    ingress:
      annotations:
        alb.ingress.kubernetes.io/group.name: external
        alb.ingress.kubernetes.io/scheme: internet-facing
      hosts:
      - host: "mimir-biz.runner-main-biz-int.infralib.entigo.io"
        paths:
        - path: "/"
          pathType: "Prefix"
