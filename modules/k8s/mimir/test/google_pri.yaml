global:
  gateway:
    name: '{{ .toptin.google-gateway.global.externalGateway | required }}'
mimir-distributed:
  ingester:
    persistentVolume:
      size: 5Gi
  store_gateway:
    persistentVolume:
      size: 5Gi
  compactor:
    persistentVolume:
      size: 5Gi
