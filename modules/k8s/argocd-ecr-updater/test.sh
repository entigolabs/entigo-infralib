#!/bin/bash
exit 0 # Disable k8s/argocd-ecr-updater tests, deprecated in favour of k8s/external-secrets
#SCRIPTPATH="$( cd -- "$(dirname "$0")" >/dev/null 2>&1 ; pwd -P )"
#cd $SCRIPTPATH
#exec $SCRIPTPATH/../../../common/test.sh "$@"
