Done. The Alloy chart now collects metrics and remote_writes them to your existing Mimir, alongside the existing log collection.

What I changed
All in entigo-infralib/modules/k8s/alloy/:

Chart.yaml — added two more alloy subchart instances next to the existing logs one: metrics (daemonset) and metricscluster (deployment). Chart.lock/charts/ regenerated via helm dependency update.
values.yaml — two new config blocks:
metrics (per-node DaemonSet): built-in prometheus.exporter.unix replaces prometheus-node-exporter (host /, /proc, /sys mounted read-only), plus scrapes the local node's kubelet and cAdvisor. Each pod only scrapes its own node (metadata.name=$HOSTNAME).
metricscluster (single-replica Deployment): scrapes kube-state-metrics and does prometheus.io/scrape annotation-based pod discovery — run as a singleton so each target is scraped exactly once. Pinned to mon/tools nodes like your other monitoring.
Both prometheus.remote_write → http://mimir-gateway.mimir/api/v1/push, with their own network policies (DNS, Mimir gateway, k8s API, kubelet:10250 / scrape-all egress).
agent_input.yaml / values-aws.yaml* — wired the Mimir endpoint via {{ .tmodule.mimir }}, priority class via {{ .module.name }}, per-cloud image registries, and the cloud-specific 443 egress (EKS 172.20.0.1/32, GKE master CIDR).
test/k8s_unit_basic_test.go — now also asserts the -metrics daemonset and -metricscluster deployment exist.
Validated: helm template (AWS + Google) and helm lint pass — rendering 2 DaemonSets, 1 Deployment, 3 NetworkPolicies, with host mounts, WAL storage path, and Mimir endpoint all correct.

What I deliberately left for follow-up (per your choices)
kube-state-metrics stays in the prometheus module for now (Alloy has no KSM equivalent) — metricscluster scrapes it cross-module during this "alongside" phase. When you retire Prometheus, KSM needs to move out into its own module/here.
Alertmanager + recording/alerting rules + blackbox-exporter are untouched (out of scope) — candidates for Mimir's ruler/alertmanager later.
Grafana still queries prometheus-server. This change only gets data into Mimir. To verify and cut over you'll want to add a Mimir datasource (http://mimir-gateway.mimir/prometheus) in the grafana module.
Recommended verification before retiring Prometheus
Deploy and check both new workloads are Running: alloy-metrics (daemonset) + alloy-metricscluster (deployment).
In Grafana, add a temporary Mimir datasource and confirm up, node_*, container_*, and kube_* series are present and dashboards 15757/15758 render.
Compare against Prometheus for a few days, then repoint Grafana → Mimir and disable the Prometheus server.
One caveat I couldn't fully verify locally: helm template checks the Go templating and YAML, but not Alloy's River semantics (the embedded config), and terratest's GetDeploymentE resolves only at CI time. The River config follows Grafana's standard patterns, but a live cluster test is the real confirmation.

Want me to also add the Mimir datasource to the grafana module so you can verify the metrics flow end-to-end?