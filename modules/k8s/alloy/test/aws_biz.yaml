logs:
  extraProcessStages: |
    stage.multiline {
      firstline     = "^[^\\s].*\\d{2}:\\d{2}:\\d{2},\\d{3}"
      max_wait_time = "5s"
      max_lines     = 5000
    }

node-metrics:
  # Exercise external_labels templating.
  externalLabels:
    cluster: aws-biz

cluster-metrics:
  # Exercise external_labels templating.
  externalLabels:
    cluster: aws-biz
