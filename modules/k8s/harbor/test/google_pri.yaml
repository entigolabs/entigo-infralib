global:
  google:
    gateway:
      name: "google-gateway-external"

harbor:
  persistence:
    imageChartStorage:
      gcs:
        bucket: "entigo{{ .tsmodule.harbor }}"
  harborAdminPassword: "Harbor12345"
