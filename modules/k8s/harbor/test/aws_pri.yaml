harbor:
  persistence:
    imageChartStorage:
      s3:
        bucket: "entigo{{ .tsmodule.harbor }}"
  harborAdminPassword: "Harbor12345"
  expose:
    ingress:
      annotations:
        alb.ingress.kubernetes.io/group.name: external
        alb.ingress.kubernetes.io/scheme: internet-facing
