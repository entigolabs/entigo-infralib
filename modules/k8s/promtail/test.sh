#!/bin/bash
exit 0 # Disable k8s/promtail tests to avoid conflict with k8s/alloy
#SCRIPTPATH="$( cd -- "$(dirname "$0")" >/dev/null 2>&1 ; pwd -P )"
#cd $SCRIPTPATH
#exec $SCRIPTPATH/../../../common/test.sh "$@"
