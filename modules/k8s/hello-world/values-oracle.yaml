# Exposed via HTTPRoute instead of Ingress (templates/httproute.yaml).
ingress:
  enabled: false

httpRoute:
  enabled: true
  gateway:
    name: oracle-gateway
    namespace: oracle-gateway
    sectionName: https
