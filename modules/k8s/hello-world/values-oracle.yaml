# Exposed via HTTPRoute instead of Ingress (templates/httproute.yaml).
ingress:
  enabled: false

httpRoute:
  enabled: true
  gateway:
    name: oracle-gateway-public
    namespace: oracle-gateway
    sectionName: https
