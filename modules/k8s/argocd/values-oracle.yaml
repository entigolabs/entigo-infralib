global:
  cloudProvider: oracle

argocd:
  configs:
    params:
      server.insecure: true
  server:
    ingress:
      # Exposed via HTTPRoute instead (templates/oracle/httpRoute.yaml).
      enabled: false
