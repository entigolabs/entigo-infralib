apiVersion: storage.gcp.upbound.io/v1beta1
kind: Bucket
metadata:
  name: change-me
spec:
  forProvider:
    location: EU
    storageClass: REGIONAL
  providerConfigRef:
    name: google-crossplane-system
