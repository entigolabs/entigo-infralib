#!/bin/bash
exit 0 # Disable aws/route53 tests to avoid conflict with aws-v2/route53

# SCRIPTPATH="$( cd -- "$(dirname "$0")" >/dev/null 2>&1 ; pwd -P )"
# cd $SCRIPTPATH
# export ENTIGO_INFRALIB_TEST_TIMEOUT="60m"
# exec $SCRIPTPATH/../../../common/test.sh "$@"
