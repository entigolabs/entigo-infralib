vpc_id: "{{ .toutput.vpc.vpc_id }}"
attachment_subnets: |
  [{{ .toptout.vpc.intra_subnets }}]
routes: |
  {
    "0.0.0.0/0" = concat({{ .toutput.vpc.private_route_table_ids }},{{ .toutput.vpc.intra_route_table_ids }})
  }
